machine:
  node:
    version: stable
general:
  artifacts:
      - ./coverage
dependencies:
  override:
    - npm i -g nyc
test:
  override:
    - nyc npm test
deployment:
  production:
    branch: master
    heroku:
      appname: learn-react-newsfeed
